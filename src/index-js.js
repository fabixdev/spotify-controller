import React from 'react';
import ReactDOM from 'react-dom/client';
import SpotifyController from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SpotifyController />
  </React.StrictMode>
);
